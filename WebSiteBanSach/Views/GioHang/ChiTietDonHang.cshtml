

@using WebSiteBanSach.Models;
@using System.Diagnostics;
@{
    ViewBag.Title = "ChiTietDonHang";
    Layout = "~/Views/Layout/NguoiDungLayout.cshtml";
}
<h2>ChiTietDonHang</h2>
<table id="divGioHang">
    <tr>
        <td>Ngày đặt</td>
        <td>Tên sách</td>

        <td>Ảnh bìa</td>

        <td>Đơn giá</td>

        <td>Số lượng</td>
    </tr>

    @{
        foreach (var ele in Model)
        {

            <tr>
                <td>@ele.GetType().GetProperty("NgayDat").GetValue(ele, null)</td>

                <td>@ele.GetType().GetProperty("TenSach").GetValue(ele, null)</td>

                <td>

                <img src="@Url.Content("~/HinhAnhSach/"+@ele.GetType().GetProperty("AnhBia").GetValue(ele,null))" width="50" height="50" /></td>

                <td>
                    @string.Format("{0:0,0}", @ele.GetType().GetProperty("DonGia").GetValue(ele, null)) VNĐ
                </td>

                <td>@ele.GetType().GetProperty("SoLuong").GetValue(ele, null)</td>


            </tr>
        }

    }

</table>
    @*<table id="divGioHang">
            <tr>
                <td>Mã sách</td>

                <td>Tên sách</td>

                <td>Ảnh bìa</td>

                <td>Đơn giá</td>

                <td>Số lượng</td>

                <td>Thành tiền</td>
            </tr>

            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.iMaSach</td>

                    <td>@item.sTenSach</td>

                    <td><a href="@Url.Action("XemChiTiet", "Sach",new { @MaSach=item.iMaSach})"><img src="@Url.Content("~/HinhAnhSach/"+item.sAnhBia)" width="50" height="50" /></a></td>

                    <td>
                        @string.Format("{0:0,0}", item.dDonGia.ToString()) VNĐ
                    </td>

                    <td>@item.iSoLuong</td>

                    <td>
                        @string.Format("{0:0,0}", item.ThanhTien.ToString()) VNĐ
                    </td>

                </tr>
            }
        </table>*@


